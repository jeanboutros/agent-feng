"""Infrastructure adapters for Agent Feng.

This module contains concrete implementations of interfaces defined in the
application layer, including MCP server clients, AI provider adapters,
and other external system integrations.

Example:
    Adapters are injected via the composition root::

        from agent_feng.infrastructure.adapters import MCPClientAdapter
"""

from __future__ import annotations
