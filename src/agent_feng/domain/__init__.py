"""Domain layer for Agent Feng.

This module contains the core business logic, entities, and value objects.
The domain layer has no external dependencies and represents the heart of
the application's business rules.

Example:
    Import domain models::

        from agent_feng.domain import models
"""

from __future__ import annotations
